// Get the subsystem
UMyNetworkSubsystem* NetworkSubsystem = GetGameInstance()->GetSubsystem<UMyNetworkSubsystem>();

// Bind to events
NetworkSubsystem->OnConnected.AddDynamic(this, &AMyActor::OnServerConnected);
NetworkSubsystem->OnMessageReceived.AddDynamic(this, &AMyActor::OnMessageReceived);
NetworkSubsystem->OnDisconnected.AddDynamic(this, &AMyActor::OnServerDisconnected);

// Connect to server
NetworkSubsystem->ConnectToServer("127.0.0.1", 7776);

// Send a message
NetworkSubsystem->SendMessage("loadPlayer|1");

// Disconnect
NetworkSubsystem->Disconnect();


"## Key Features
1. **Automatic connection management** - Handles socket creation and cleanup
2. **Event-driven architecture** - Uses delegates for connection events and incoming messages
3. **Automatic message receiving** - Polls for incoming data every 0.01 seconds
4. **Blueprint support** - All functions are exposed to Blueprints
5. **Proper lifecycle management** - Cleans up on subsystem destruction

This implementation should work with UE 5.5 and provides a clean, reusable way to connect to third-party socket servers!"


++

   void AYourActor::BeginPlay()
   {
       Super::BeginPlay();
       
       UMyNetworkSubsystem* NetSys = GetGameInstance()->GetSubsystem<UMyNetworkSubsystem>();
       if (NetSys)
       {
           NetSys->ConnectToServer("127.0.0.1", 7776);
       }
   }